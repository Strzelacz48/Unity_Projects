using UnityEngine;

public class PlayerMovement : MonoBehaviour
{
    double upwardForce=100f;
    bool DKey=false,Wkey=false,AKeay=false;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if(GetKeyDown)
    }

    void FixedUpdate()
    {

    }
}
